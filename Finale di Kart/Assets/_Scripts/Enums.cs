namespace _Scripts
{
    public enum NodeType
    {
        None,
        Server,
        Client,
    }


    public enum ChallengeState {
        Rejected,
        Accepted,
    }

    public enum ChallengeResponse {
        Accept,
        Reject,
    }
}